import Carrier from './Carrier';
import { PublicListing, SubscriberListing } from './Listing';

export const appResolverMaps = {
  Carrier,
  PublicListing,
  SubscriberListing,
};

export const publicResolverMaps = {
  Carrier,
  PublicListing,
};
