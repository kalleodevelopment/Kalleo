import normalizeFont from '../helpers/normalize-fonts';

const styles = {
  $fontMini: normalizeFont(12),
  $fontSmall: normalizeFont(14),
  $fontMedium: normalizeFont(18),
  $fontLarge: normalizeFont(22),
  $fontXLarge: normalizeFont(26),
  $fontXXLarge: normalizeFont(28),
  $fontHeader: normalizeFont(40),
};

export default styles;
